"use strict";

var _interopRequireDefault = require("@babel/runtime/helpers/builtin/interopRequireDefault");

var _chai = require("chai");

var _mod = _interopRequireDefault(require("./mod"));

describe('mod', function () {
  it('should work when the integer is zero', function () {
    _chai.assert.strictEqual((0, _mod.default)(0, 4), 0);
  });
  it('should work when the integer is withing the bounds', function () {
    _chai.assert.strictEqual((0, _mod.default)(1, 4), 1);
  });
  it('should work when the integer is above the bounds', function () {
    _chai.assert.strictEqual((0, _mod.default)(-1, 4), 3);
  });
  it('should work when the integer is under the bounds', function () {
    _chai.assert.strictEqual((0, _mod.default)(6, 4), 2);
  });
});